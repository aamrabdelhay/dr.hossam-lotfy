import {NextResponse} from 'next/server';import {z} from 'zod';import {prisma} from '@/lib/prisma';import {user} from '@/lib/api';import {createOfficeRequest,notifySenior,isSeniorManagement} from '@/lib/office-workflow';
export async function PATCH(req:Request,{params}:{params:Promise<{id:string}>}){const session=await user();if(!session)return NextResponse.json({error:'يجب تسجيل الدخول'},{status:401});const{id}=await params;const data=z.object({status:z.enum(['POTENTIAL','MAIN','FOLLOW_UP','REJECTED']),reason:z.string().max(1000).optional()}).parse(await req.json());const senior=await isSeniorManagement(session);if(!senior&&session.role==='lawyer'){const rid=await createOfficeRequest({type:'CLIENT_STATUS',title:'طلب تعديل حالة عميل',reason:data.reason||`طلب تغيير حالة العميل إلى ${data.status}`,payload:{clientId:id,status:data.status,reason:data.reason},targetEntityType:'client',targetEntityId:id,session});await notifySenior('طلب تعديل حالة عميل',`هناك طلب لتغيير حالة عميل إلى ${data.status}.`,'/admin/office').catch(()=>undefined);return NextResponse.json({ok:true,requiresApproval:true,requestId:rid});}if(data.status==='REJECTED')await prisma.$executeRawUnsafe(`UPDATE "clients" SET "status"='REJECTED',"rejection_reason"=$2,"archived_at"=NOW() WHERE "id"=$1`,id,data.reason??null);else await prisma.$executeRawUnsafe(`UPDATE "clients" SET "status"=$2,"archived_at"=NULL,"rejection_reason"=NULL WHERE "id"=$1`,id,data.status);return NextResponse.json({ok:true});}
