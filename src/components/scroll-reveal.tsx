'use client';
import * as React from 'react';
export function ScrollReveal(){React.useEffect(()=>{const nodes=Array.from(document.querySelectorAll<HTMLElement>('main section, main article, main [data-scroll-reveal]'));if(window.matchMedia('(prefers-reduced-motion: reduce)').matches){nodes.forEach(n=>n.classList.add('scroll-reveal-visible'));return}const observer=new IntersectionObserver(entries=>{entries.forEach(entry=>{if(entry.isIntersecting){entry.target.classList.add('scroll-reveal-visible');observer.unobserve(entry.target)}})},{threshold:.08,rootMargin:'0px 0px -8% 0px'});nodes.forEach(n=>{n.classList.add('scroll-reveal');observer.observe(n)});return()=>observer.disconnect()},[]);return null;}