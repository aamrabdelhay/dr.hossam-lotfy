'use client'; import * as React from 'react'; import {useRouter} from 'next/navigation'; import {Loader2,Plus,RotateCcw} from 'lucide-react'; import {Button,Field,Input,Modal,Textarea} from '../ui'; import {ComboboxWithAdd} from '../combobox-with-add'; import {MultiLawyerSelector} from './multi-lawyer-selector'; import {toastSuccess,toastError} from '../toasts'; import type {NavLocation} from '@/lib/constants';
export function TaskCreator({open,onClose,locations,lawyers,cases,defaultDate,onCreated}:{open:boolean;onClose:()=>void;locations:NavLocation[];lawyers:Array<{id:string;name:string;isPrincipal?:boolean}>;cases:Array<{id:string;name:string;number:string}>;defaultDate?:string;onCreated?:()=>void|Promise<void>}){const router=useRouter();const[locationId,setLocationId]=React.useState(''),[caseId,setCaseId]=React.useState(''),[clientName,setClientName]=React.useState(''),[description,setDescription]=React.useState(''),[notes,setNotes]=React.useState(''),[date,setDate]=React.useState(''),[time,setTime]=React.useState(''),[lawyerIds,setLawyerIds]=React.useState<string[]>([]),[busy,setBusy]=React.useState(false),[createdCount,setCreatedCount]=React.useState(0);const resetForm=React.useCallback(()=>{setLocationId('');setCaseId('');setClientName('');setDescription('');setNotes('');setDate(defaultDate??'');setTime('');setLawyerIds([])},[defaultDate]);React.useEffect(()=>{if(open){resetForm();setCreatedCount(0)}},[open,resetForm]);const selectedCase=cases.find(c=>c.id===caseId);const submit=async(e:React.FormEvent)=>{e.preventDefault();if(!description.trim())return toastError('اسم التكليف مطلوب.');setBusy(true);try{const res=await fetch('/api/tasks',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({locationId:locationId||undefined,caseName:selectedCase?.name||undefined,caseNumber:selectedCase?.number||undefined,clientName:clientName.trim()||undefined,description:description.trim(),notes:notes.trim()||undefined,scheduledDate:date||undefined,scheduledTime:time||undefined,lawyerIds:lawyerIds.length?lawyerIds:undefined})});const d=await res.json().catch(()=>({}));if(!res.ok)throw new Error(d?.error||`فشل الطلب (${res.status})`);setCreatedCount(v=>v+1);toastSuccess('تم حفظ التكليف ✓');router.refresh();await onCreated?.();resetForm()}catch(err){toastError(err instanceof Error?err.message:'تعذر إنشاء التكليف.')}finally{setBusy(false)}};const addLocation=async(label:string):Promise<string|void>=>{try{const res=await fetch('/api/locations',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({name:label,type:'OTHER'})});const d=await res.json().catch(()=>({}));if(res.ok&&d?.location?.id){toastSuccess('تمت إضافة المكان ✓');router.refresh();return d.location.id as string}toastError(d.error??`تعذر إضافة الجهة (${res.status}).`)}catch{toastError('تعذر الاتصال بالخادم أثناء إضافة الجهة.')}};const addCase=async(label:string):Promise<string|void>=>{try{const res=await fetch('/api/cases',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({name:label,number:`AUTO-${Date.now()}`})});const d=await res.json().catch(()=>({}));if(res.ok&&d?.case?.id)return d.case.id as string;toastError(d.error??`تعذر إضافة القضية (${res.status}).`)}catch{toastError('تعذر الاتصال بالخادم أثناء إضافة القضية.')}};return <Modal open={open} onClose={onClose} title="إضافة تكليف" wide footer={<div className="flex flex-wrap items-center justify-between gap-2"><div className="text-[11px] font-bold text-navy-400">{createdCount?`تم حفظ ${createdCount} تكليف`:'التكليف مستقل عن القضية — اسم التكليف هو الحقل الإلزامي الوحيد'}</div><div className="flex gap-2">{createdCount>0&&<Button variant="outline" onClick={resetForm}><RotateCcw size={14}/>تكليف آخر</Button>}<Button variant="ghost" onClick={onClose}>إغلاق</Button><Button type="submit" form="task-creator-form" disabled={busy}>{busy?<Loader2 size={14} className="animate-spin"/>:<Plus size={15}/>} حفظ التكليف</Button></div></div>}><form id="task-creator-form" onSubmit={submit} className="space-y-4"><Field label="اسم التكليف" required status={!!description.trim()}><Textarea name="description" value={description} onChange={e=>setDescription(e.target.value)} placeholder="مثال: مراجعة ملف القضية أو إعداد مذكرة دفاع"/></Field><div className="grid gap-4 sm:grid-cols-2"><Field label="المحامي / المحامون" status={lawyerIds.length>0}><MultiLawyerSelector lawyers={lawyers} selected={lawyerIds} onChange={setLawyerIds}/></Field><Field label="المحكمة / جهة حكومية" status={!!locationId}><ComboboxWithAdd id="task-location" name="locationId" ariaLabel="المحكمة أو الجهة الحكومية" placeholder="اختياري" options={locations.map(l=>({value:l.id,label:l.name}))} value={locationId} onChange={setLocationId} onAdd={addLocation}/></Field><Field label="القضية" status={!!caseId}><ComboboxWithAdd id="task-case" name="caseId" ariaLabel="القضية" placeholder="اختياري" options={cases.map(c=>({value:c.id,label:`${c.name} — ${c.number}`}))} value={caseId} onChange={setCaseId} onAdd={addCase}/></Field><Field label="اسم العميل" status={!!clientName.trim()}><Input value={clientName} onChange={e=>setClientName(e.target.value)} placeholder="اختياري"/><span className="text-[9px] font-semibold text-navy-300">اختياري — لا يحول التكليف إلى قضية.</span></Field><Field label="التاريخ" status={!!date}><Input type="date" name="scheduledDate" value={date} onChange={e=>setDate(e.target.value)}/></Field><Field label="الساعة" status={!!time}><Input type="time" name="scheduledTime" value={time} onChange={e=>setTime(e.target.value)}/></Field></div><Field label="ملاحظات" status={!!notes.trim()}><Textarea name="notes" value={notes} onChange={e=>setNotes(e.target.value)} className="min-h-[60px]" placeholder="اختياري"/></Field></form></Modal>}
