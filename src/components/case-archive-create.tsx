'use client';
import * as React from 'react'; import {FilePlus2} from 'lucide-react'; import {Button,Card,Field,Input,Select} from './ui'; import {toastError,toastSuccess} from './toasts';
const fields=[['year','سنة القضية','مثال: 2021'],['court','المحكمة','مثال: محكمة جنوب القاهرة'],['circuit','الدائرة','مثال: الدائرة 5 مدني'],['caseType','نوع القضية / التصنيف','مثال: مدني، تجاري، جنائي'],['plaintiff','المدعي','اسم المدعي'],['defendant','المدعى عليه','اسم المدعى عليه'],['responsibleLawyer','المحامي المسؤول','اسم المحامي'],['status','حالة القضية','مثال: انتهت، مؤجلة، حكم نهائي'],['filingDate','تاريخ القيد / بداية القضية','YYYY-MM-DD'],['lastActionDate','تاريخ آخر إجراء','YYYY-MM-DD'],['judgmentDate','تاريخ الحكم','YYYY-MM-DD'],['judgmentResult','نتيجة القضية / الحكم','الحكم أو النتيجة باختصار']] as const; type FormState=Record<(typeof fields)[number][0]|'name'|'number'|'clientName'|'description'|'clientId',string>; const emptyForm:FormState={name:'',number:'',clientName:'',description:'',clientId:'',year:'',court:'',circuit:'',caseType:'',plaintiff:'',defendant:'',responsibleLawyer:'',status:'',filingDate:'',lastActionDate:'',judgmentDate:'',judgmentResult:''};
export function CaseArchiveCreate(){const[open,setOpen]=React.useState(false),[busy,setBusy]=React.useState(false),[form,setForm]=React.useState<FormState>(emptyForm),[clients,setClients]=React.useState<Array<{id:string;name:string}>>([]);const set=(k:keyof FormState,v:string)=>setForm(p=>({...p,[k]:v}));React.useEffect(()=>{if(open)fetch('/api/clients').then(r=>r.ok?r.json():null).then(d=>d&&setClients((d.clients||[]).map((c:any)=>({id:c.id,name:c.name})))).catch(()=>undefined)},[open]);const submit=async(e:React.FormEvent)=>{e.preventDefault();if(!form.name.trim()||!form.number.trim()||busy)return;setBusy(true);try{const res=await fetch('/api/cases',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify(form)});const data=await res.json().catch(()=>({}));if(!res.ok){toastError(data.error??'تعذر إضافة القضية.');return}toastSuccess('تمت إضافة القضية القديمة إلى الأرشيف ✓');setForm(emptyForm);setOpen(false);window.location.reload()}catch{toastError('تعذر الاتصال بالخادم. حاول مرة أخرى.')}finally{setBusy(false)}};return <div><Button size="sm" variant="gold" onClick={()=>setOpen(v=>!v)}><FilePlus2 size={14}/>إضافة قضية قديمة</Button>{open&&<Card className="mt-3 p-4"><div className="mb-4"><h2 className="text-[14px] font-extrabold text-navy-900">إضافة قضية قديمة إلى الأرشيف</h2><p className="mt-1 text-[11px] font-semibold text-navy-400">اسم القضية ورقم القضية فقط إلزاميان.</p></div><form onSubmit={submit} className="grid gap-3 sm:grid-cols-2"><Field label="اسم القضية"><Input value={form.name} onChange={e=>set('name',e.target.value)} required/></Field><Field label="رقم القضية"><Input value={form.number} onChange={e=>set('number',e.target.value)} required dir="ltr"/></Field><Field label="العميل"><Select value={form.clientId} onChange={e=>set('clientId',e.target.value)}><option value="">بدون ربط</option>{clients.map(c=><option key={c.id} value={c.id}>{c.name}</option>)}</Select></Field><Field label="اسم العميل — إذا لم يكن مسجلاً"><Input value={form.clientName} onChange={e=>set('clientName',e.target.value)} placeholder="اختياري"/></Field>{fields.map(([key,label,placeholder])=><Field key={key} label={label}><Input value={form[key]} onChange={e=>set(key,e.target.value)} placeholder={`${placeholder} — اختياري`} dir={key.includes('Date')||key==='year'?'ltr':undefined} type={key.includes('Date')?'date':'text'}/></Field>)}<Field label="ملاحظات تاريخية"><textarea value={form.description} onChange={e=>set('description',e.target.value)} rows={4} className="w-full rounded-xl border border-navy-200 bg-white px-3 py-2.5 text-[13px] text-navy-900"/></Field><div className="flex items-end gap-2 sm:col-span-2"><Button type="submit" size="sm" disabled={busy}>{busy?'جارٍ الحفظ…':'حفظ في الأرشيف'}</Button><Button type="button" size="sm" variant="ghost" onClick={()=>setOpen(false)} disabled={busy}>إلغاء</Button></div></form></Card>}</div>}
